class Solution:
    def uniquePaths(self, m: int, n: int) -> int:
        L = [[1] * (n) for _ in range(m)]

        for i in range(1, m):
            for j in range(1, n):
                L[i][j] = L[i][j - 1] + L[i - 1][j]

        return L[m - 1][n - 1] 
